---
sudo: required

language: python

services:
  - docker

script:
  - docker-compose up -d

deploy:
  provider: releases
    api_key: $GITHUB_OAUTH_TOKEN
    file: README.md
    skip_cleanup: true
    on:
      tags: true
